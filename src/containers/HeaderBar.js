import React, { Component } from 'react';

export default class HeaderBar extends Component {
  constructor(props) {
    super(props)
  }

  render() {
    return (
      <div>header bar</div>
    );
  }
}